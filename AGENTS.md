# Agent Startup Notes

This repository keeps the short, current project memory in `README.md`.

Before doing work in a fresh Codex session, after context compaction, or after
a long interruption:

1. Read `README.md` first.
2. Read `MIGRATION_CONTEXT.md` when migration history, board decisions, FSBL,
   EML, APP boot flow, or peripheral ownership are relevant.
3. For PCMD3180/audio work, also inspect `docs/knowledge` and the H7 reference
   project at `D:\Project\NECCS\H7_Original\firmware\NECCS`.
4. If chat context, compressed context, and repository docs disagree, trust the
   repository docs only after checking the current files and `git status`.
5. After important build/debug findings, update `README.md` so the next session
   can recover quickly.

Keep this file small. Put detailed notes in `README.md` or
`MIGRATION_CONTEXT.md`.
